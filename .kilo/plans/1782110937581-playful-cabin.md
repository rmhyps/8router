# Merge Plan: Upstream v0.5.6–v0.5.8 → VansAI (main)

## Ringkasan Situasi

- **Upstream:** `f2a7ae20` → `0c47c891` (30 commit, tag v0.5.6, v0.5.7, v0.5.8)
- **Kita:** `f2a7ae20` → `a9ca3186` (75 commit, banyak formatting + fitur custom)
- **Konflik:** 21 file
- **Auto-merged:** 20 file (Git bisa merge otomatis)
- **New upstream files:** ~80 file baru (codebuddy-cn, headroom, ponytail upstream, xiaomi, dll)

---

## Phase 0: Commit & Push Non-Conflict (Clean State)

Working tree sudah bersih (tidak ada uncommitted changes). Skip phase ini.

---

## Phase 1: Feature Gap Analysis — CUSTOM_LOGIC.md

Membandingkan setiap konflik dengan dokumentasi di `CUSTOM_LOGIC.md`:

### ✅ Sudah Terdokumentasi di CUSTOM_LOGIC.md

| # | Fitur | File Konflik | Section |
|---|-------|-------------|---------|
| 1 | ACL Enforcement | `src/sse/handlers/chat.js` (auto-merged) | §1 |
| 2 | Ponytail System Prompt Injection | `open-sse/rtk/ponytail.js`, `open-sse/handlers/chatCore.js` | §2 |
| 3 | Skeptical AI Rules | `open-sse/rtk/ponytailPrompts.js` (no conflict) | §3 |
| 4 | allowRemoteNoApiKey Dashboard Guard | `EndpointPageClient.js`, `settingsRepo.js` | §6 |
| 5 | Combo.js Fixes | `open-sse/services/combo.js` (auto-merged) | §8 |
| 6 | VansAI Branding | `README.md`, `Sidebar.js` | §9 |

### ❌ Belum Terdokumentasi — Perlu Ditambah ke CUSTOM_LOGIC.md

| # | Fitur | Penjelasan | File Terkait |
|---|-------|-----------|-------------|
| A | **ZCode Start Plan Provider** | Provider custom `zcode` + executor + OAuth config. **Tidak ada di upstream.** | `open-sse/executors/zcode.js`, `open-sse/executors/index.js` (ZcodeExecutor), `open-sse/providers/registry/zcode.js`, `src/lib/oauth/constants/oauth.js` (ZAI_CONFIG) |
| B | **Multiple Connections Per Compatible Node** | Allow >1 koneksi per provider node yang compatible. **Tidak ada di upstream.** | `src/app/api/providers/route.js` (commit 932698a2) |

### ℹ️ Sudah Terdokumentasi / Bukan Custom (Upstream Feature)

| # | Fitur | Status |
|---|-------|--------|
| C | Provider Custom Models Storage (`providerCustomModels.js`) | **Upstream feature** (commit 707a9155). Bukan custom logic kita. Auto-merged. |
| D | Indonesian TTS Voices | Sudah terdokumentasi §5, auto-merged |
| E | Translator Fixes | Sudah terdokumentasi §7, auto-merged |

**Action:** Tambah section baru ke CUSTOM_LOGIC.md untuk:
- §11: ZCode Start Plan Provider (OAuth + executor)
- §12: Multiple Connections Per Compatible Node

---

## Phase 2: Strategi Merge Per File Konflik

### Grup A: Dokumen (ambil upstream + custom branding kita)

| File | Strategi |
|------|----------|
| **DOCKER.md** | Accept upstream (Headroom sidecar docs). Kita hanya reformat. → `--theirs` lalu append custom jika perlu |
| **README.md** | Keep our VansRoute branding header + accept upstream's Headroom/Ponytail feature rows |

### Grup B: Caveman/Ponytail Refactoring (upstream extract → systemInject.js)

Upstream melakukan refactoring yang bagus: extract inline injection logic dari `caveman.js` ke shared `systemInject.js`. Ponytail upstream juga pakai `systemInject.js`.

**Ponytail file naming:**
- Upstream: `ponytailPrompt.js` (singular) — more compact prompts, no skeptical rules
- Kita: `ponytailPrompts.js` (plural) — enhanced prompts with `SHARED_SKEPTICAL`, `SHARED_BOUNDARIES`, `SHARED_PERSISTENCE`
- Keduanya export `PONYTAIL_PROMPTS` (nama export sama!)
- Setelah merge, kedua file akan ada. Kita pakai yang kita (enhanced) via ponytail.js.

| File | Strategi |
|------|----------|
| **open-sse/rtk/caveman.js** | Accept upstream refactoring (delegates to `systemInject.js`). Export `injectSystemPrompt` from `systemInject.js` langsung. |
| **open-sse/rtk/ponytail.js** | Use upstream style (`systemInject.js`) TAPI import dari `ponytailPrompts.js` (plural, versi enhanced kita). |
| **open-sse/rtk/ponytailPrompt.js** | File upstream baru (auto-merged, singular). Biarkan ada — tidak dipakai oleh ponytail.js kita. Bisa dihapus di cleanup nanti. |
| **open-sse/rtk/ponytailPrompts.js** | File kita (plural, enhanced). Tetap dipertahankan. |
| **open-sse/rtk/systemInject.js** | File baru dari upstream (auto-merged). Terima as-is. |

### Grup C: Core Handler

| File | Upstream Change | Our Change | Strategi |
|------|----------------|------------|----------|
| **open-sse/handlers/chatCore.js** | +headroom params, +ponytail params, +resolveTransport, +imageGen detection, +headroom compression | +ponytail injection section | **Merge manual:** accept upstream params (headroom, resolveTransport, imageGen) + keep our ponytail injection block (sudah ada di upstream juga, jadi pakai upstream's version yang lebih lengkap karena sudah include ponytailEnabled/ponytailLevel params) |
| **open-sse/handlers/imageGenerationCore.js** | +executor-delegating adapter flow (41 baris) | Full file reformat only (189+/189-) | Accept upstream + apply our formatting style |
| **open-sse/executors/index.js** | +CodeBuddyExecutor import/register | +ZcodeExecutor import/register | **Merge manual:** keep BOTH — CodeBuddy + Zcode executor imports and registrations |

### Grup D: CLI

| File | Strategi |
|------|----------|
| **cli/hooks/sqliteRuntime.js** | Accept upstream (sql.js wasm validation, SQL_JS_VERSION). Kita hanya reformat. |
| **cli/src/cli/menus/settings.js** | Accept upstream (+headroom toggle in CLI menu). Kita hanya reformat. |

### Grup E: Dashboard UI

| File | Strategi |
|------|----------|
| **EndpointPageClient.js** | **Merge manual:** accept upstream's headroom/ponytail UI state + keep our `allowRemoteNoApiKey` toggle UI. Kedua fitur orthogonal. |
| **endpointConstants.js** | **Merge manual:** upstream adds `PONYTAIL_LEVELS`, kita juga adds `PONYTAIL_LEVELS`. Bandingkan desc text → pakai upstream's version (lebih deskriptif) atau merge best of both. |
| **CompatibleModelsSection.js** | Accept upstream (refactored to use `providerCustomModels` util). Kita hanya reformat. |
| **PassthroughModelsSection.js** | Accept upstream (sama seperti Compatible — refactored ke util). Kita hanya reformat. |
| **Sidebar.js** | **Merge manual:** upstream renames "Endpoint" → "Endpoint & Key". Keep our full reformat + apply upstream label rename. |

### Grup F: API Routes

| File | Strategi |
|------|----------|
| **src/app/api/cli-tools/claude-settings/route.js** | Accept upstream (JSONC tolerance + silent error). Kita hanya reformat. |
| **src/app/api/oauth/[provider]/[action]/route.js** | Accept upstream (`codebuddy` → `codebuddy-cn` rename). Kita hanya reformat. |
| **src/app/api/provider-nodes/validate/route.js** | Accept upstream (+SSRF guard via `assertPublicUrl` + `isLocalRequest`). Cek dependency `ssrfGuard.js` sudah ada. |
| **src/app/api/providers/route.js** | **Merge manual:** accept upstream's `supportsApiKeyMode` check + keep our multi-connection logic (commit 932698a2). |
| **src/app/api/settings/route.js** | Accept upstream (+PROTECTED_SETTING_KEYS for password/mitmSudoEncrypted). Cek apakah ini konflik dengan custom logic kita. |

### Grup G: Lib/Services

| File | Strategi |
|------|----------|
| **src/lib/db/repos/settingsRepo.js** | **Merge manual:** accept upstream's headroom defaults (`headroomEnabled`, `headroomUrl`, `headroomCompressUserMessages`) + keep our `allowRemoteNoApiKey: false`. |
| **src/lib/oauth/constants/oauth.js** | **Merge manual:** accept upstream's AWS_REGION_PATTERN + `codebuddy-cn` rename + keep our `ZAI_CONFIG` / `ZCODE` provider. |

---

## Phase 3: Execution Steps

```
1. Update CUSTOM_LOGIC.md (add §11 ZCode Provider, §12 Multi-Connection)

2. git merge upstream/master
   → Auto-resolves 20 file

3. Resolve 21 conflicts manually per Phase 2 strategy:

   GROUP A (docs — simple):
     a. DOCKER.md → git checkout --theirs DOCKER.md && git add DOCKER.md
     b. README.md → manual: keep VansRoute header + upstream feature rows

    GROUP B (caveman/ponytail refactor):
     c. open-sse/rtk/caveman.js → git checkout --theirs (accept upstream refactor)
     d. open-sse/rtk/ponytail.js → manual: upstream style (`systemInject.js`) + import dari `ponytailPrompts.js` (plural, kita)

   GROUP C (core handler):
     e. open-sse/handlers/chatCore.js → git checkout --theirs (upstream sudah include ponytail params)
        LALU verifikasi injectPonytail section ada + pakai ponytailPrompts.js bukan ponytailPrompt.js
     f. open-sse/handlers/imageGenerationCore.js → git checkout --theirs + reformat
     g. open-sse/executors/index.js → manual: keep BOTH CodeBuddy + Zcode

   GROUP D (CLI):
     h. cli/hooks/sqliteRuntime.js → git checkout --theirs + reformat
     i. cli/src/cli/menus/settings.js → git checkout --theirs + reformat

   GROUP E (UI):
     j. EndpointPageClient.js → manual: theirs + add back allowRemoteNoApiKey toggle
     k. endpointConstants.js → manual: use upstream PONYTAIL_LEVELS (atau merge)
     l. CompatibleModelsSection.js → git checkout --theirs + reformat
     m. PassthroughModelsSection.js → git checkout --theirs + reformat
     n. Sidebar.js → manual: keep our format + rename "Endpoint" → "Endpoint & Key"

   GROUP F (API routes):
     o. claude-settings/route.js → git checkout --theirs + reformat
     p. oauth/[provider]/[action]/route.js → git checkout --theirs + reformat
     q. provider-nodes/validate/route.js → git checkout --theirs + reformat
     r. providers/route.js → manual: theirs + add back multi-connection logic
     s. settings/route.js → git checkout --theirs + reformat

   GROUP G (lib):
     t. settingsRepo.js → manual: theirs + add allowRemoteNoApiKey: false
     u. oauth/constants/oauth.js → manual: theirs + add ZAI_CONFIG + ZCODE provider

4. Post-merge verification:
     - Verify ponytailPrompts.js (plural) import di ponytail.js
     - Verify ssrfGuard.js dependency exists (untuk provider-nodes validate)
     - Verify zcode executor + registry files exist
     - pnpm run build → verify no build errors
     - cd tests && npx vitest run → verify tests pass

5. Commit merge:
     git add .
     git commit -m "merge: upstream v0.5.6–v0.5.8 — preserve ZCode, ACL, allowRemoteNoApiKey, ponytail"

6. Push:
     git push origin main
```

---

## Phase 4: Post-Merge Cleanup

- **Hapus `ponytailPrompt.js` (singular, upstream)** setelah merge selesai — kita pakai `ponytailPrompts.js` (plural, enhanced). Tidak ada import lain yang pakai yang singular karena ponytail.js kita sudah diupdate.
- **Update CUSTOM_LOGIC.md file manifest** untuk reflect new files dari upstream: `systemInject.js`, `headroom.js`, `codebuddy-cn.js`, `ponytailPrompt.js` (dihapus), `xiaomi-tokenplan.js`, `opencode-go.js`, dll.
- **Update CUSTOM_LOGIC.md §2 (Ponytail):** ubah import path dari `caveman.js` → `systemInject.js` karena refactoring upstream.

---

## Risiko & Caveats

1. **Formatting rewrites:** Banyak file kita yang full rewrite (189+/189-). Accept `--theirs` + reformat berarti kehilangan our formatting style. Ini acceptable karena formatting tidak mempengaruhi fungsi.
2. **ponytailPrompt.js vs ponytailPrompts.js:** Upstream pakai singular, kita plural. Perlu dipastikan import konsisten setelah merge.
3. **SSRF Guard dependency:** `provider-nodes/validate/route.js` sekarang import `@/shared/utils/ssrfGuard.js` — file ini baru dari upstream, harus auto-merged.
4. **Multi-connection logic:** Commit 932698a2 di `providers/route.js` perlu dire-apply manual setelah accept upstream's `supportsApiKeyMode` change.
